services:
  gateway:
    build:
      context: ./Gateway
      dockerfile: Dockerfile
    ports:
      - "7057:8080"
    environment:
      ASPNETCORE_ENVIRONMENT: Development
      GeoCode__ApiKey: ${GEOCODE_API_KEY}
